// Este archivo es un remanente de una estructura anterior y no está en uso.
// El componente principal de la aplicación se encuentra en `screens/MainApp.tsx`.
// Es seguro eliminar este archivo.
import React from 'react';

const MainApp: React.FC = () => {
  return null;
};

export default MainApp;
