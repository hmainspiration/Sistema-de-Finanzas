// Este archivo es un remanente de una estructura anterior y no está en uso.
// El componente de selección de versión activo se encuentra en `screens/VersionSelectionScreen.tsx`.
// Es seguro eliminar este archivo.
import React from 'react';

const VersionSelectionScreen: React.FC = () => {
  return null;
};

export default VersionSelectionScreen;
